### Environmental Variables ###

The following environmental variables can be specified either directly in the
environment or in a `.env` file in the root folder of the project:

```
PORT=3030                   # The port the API is served on

STYTCH_PROJECT_ID=xyz       # Authentication Project Id
STYTCH_SECRET=xyz           # Authentication Secret
```